# Interactive Animated Sprite Demo

A TypeScript browser application built with Phaser.  
The app displays a 1000×600 game area with a background and a looping animated sprite.  
The sprite smoothly moves to the clicked or touched position using a nonlinear trajectory.

The project demonstrates object-oriented TypeScript architecture, Phaser animation, input handling, scaling, fullscreen fallback behavior, and browser compatibility considerations.